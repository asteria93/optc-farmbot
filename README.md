# OPTC Farming Bot

WINDOWS:
1. Double-clic `setup.bat`
2. Double-clic `start.bat`
3. Va à `http://localhost:5000`

MAC/LINUX:
1. `bash setup.sh`
2. `bash start.sh`
3. Va à `http://localhost:5000`

Prérequis:
- Python installé
- Redis installé et disponible dans le terminal (`redis-server`)
